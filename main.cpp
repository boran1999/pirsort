#include<iostream>
#include "sort.h"
#include <ctime>
#include <stdlib.h>

using namespace std;

int main(void) {
	/*
	SortableVector<int> vec;
	srand((unsigned)time(0));
	for (int i = 0; i < 10; i++) {
		vec.push_back(rand());
		cout << vec[i]<<" ";
	}
	cout << endl;
	vec.sort();
	for (int i = 0; i < 10; i++) {	
		cout << vec[i] << " ";
	}
	*/
	char a[] = "AAAAA";
	system("pause");
	return 0;
}
